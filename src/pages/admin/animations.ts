// Re-export shared animation factories from the lib layer.
// Admin components import from this path; the canonical source is @/lib/animations.
export { fadeUp, fadeIn, scaleIn, tabTransition } from "@/lib/animations";
